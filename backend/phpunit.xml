<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/11.0/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         executionOrder="depends,defects"
         beStrictAboutOutputDuringTests="true"
         cacheDirectory=".phpunit.cache"
         requireCoverageMetadata="false">
    <!-- Reports "the database is not running" once, before the Feature suite
         starts, instead of letting all 748 of its tests fail one by one on the
         same unreachable host. Unit runs are unaffected. -->
    <extensions>
        <bootstrap class="Tests\Support\RequireDatabase"/>
    </extensions>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>src</directory>
        </include>
    </source>
    <!-- Reports are only produced when a coverage driver (pcov/xdebug) is
         loaded; CI enables pcov and enforces a floor via
         scripts/check-coverage.php. Without a driver PHPUnit just warns. -->
    <coverage includeUncoveredFiles="true">
        <report>
            <clover outputFile="coverage/clover.xml"/>
        </report>
    </coverage>
</phpunit>
