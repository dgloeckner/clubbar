# Implementation Plans Index

This index tracks the status of all implementation plans for Club Bar.

---

## Current Plan

| Plan | Status | Summary |
|------|--------|---------|
| [Terminal Auto-Update](./2026-09-06-terminal-auto-update.md) | Implemented (M1–M7 done and each verified; backend Unit 3109/3109, Flutter 1056/1056, `updater-flow.sh` 63/63 and `updater-version.sh` 25/25, vitest and the new `api-tests` spec green. M6 — the `release-gate` job — only runs on a `release` event, so it is verified by construction rather than exercised) | [#318](https://github.com/dgloeckner/clubbar/issues/318): terminals were updated by hand, so they were updated when something was already wrong. A terminal now installs **exactly the version its backend reports** ([ADR-0054](../adr/0054-terminal-runs-its-backends-version.md)) — A/B slots with an atomic swap, SHA-256 verification, a heartbeat watchdog with rollback and a never-retried `blocked` list, and `X-Terminal-Version` on every sync so a frozen terminal is visible instead of silent |
| [Member Dialog: One Status Strip, Sticky Save](./2026-09-05-member-dialog-status-strip.md) | Implemented (M1–M6 done and each verified; vitest 616/616, `admin-chromium` 401/402 with the one failure a pre-existing shared-club-config race between the settings specs — it passes in isolation and a *different* settings spec fails on a re-run. `admin-mobile` could not run in this session: the sandbox's egress policy 403s WebKit's download host, so its two new specs were verified against Chromium at `devices['iPhone 14']`) | [#830](https://github.com/dgloeckner/clubbar/issues/830): the *Mitglied bearbeiten* dialog had four independent green indicators — a requirements panel (#629), a SEPA alert (#392), a `✓ Pflicht` pill per satisfied field and blue `→ erforderlich für …` pills — none of which answered the three questions an admin opens it with, and it was ~1750px tall inside one scrolling box, so *Speichern* was ~800px below the fold on a 900px screen. One strip grouped by **outcome** (Terminal · SEPA-Einzug · Erreichbar), previewing the save rather than reporting the load; markers only where something is missing; explanations behind an info icon; and a three-band modal whose footer never moves. Design: [`docs/reviews/2026-09-05-member-edit-dialog/`](../docs/reviews/2026-09-05-member-edit-dialog/) (PR [#831](https://github.com/dgloeckner/clubbar/pull/831)) |
| [Member Self-Registration via QR Code](./2026-08-31-member-self-registration.md) | Implemented (M1–M9 done and each verified, landing as a stacked queue onto `feat/user-onboarding` — PRs #785, #787–#792 and #793; backend Unit 2926 green, `admin-chromium`+`register` 400/400 and `api-tests` 804/804 at 4 workers. M8 gave the onboarding page the club's mail design and its identity — masthead, mark and footer from the same two rows the mail reads; M9 hardened the mobile download — an automatic save, the phone's share sheet, a done screen that survives a reload, and the two sentences an applicant was missing about the mail and the 30-day purge) | [#776](https://github.com/dgloeckner/clubbar/issues/776), [#777](https://github.com/dgloeckner/clubbar/issues/777): onboarding is a transcription exercise today — the applicant writes an IBAN on paper and a Kassenwart types it back in (UC-A11), with a mod-97 checksum as its only defence, while ADR-0020 keeps the new member off the terminal until that happens. A member scans a QR poster, types their own details behind a URL secret carried in the **fragment** (UC-A68's reasoning: a request line is logged, a fragment is never sent), and the submission is sealed into a pending store with the exact `mandates` column shape — ADR-0036 gets no exception, and approval moves the ciphertext **verbatim**. **A pending registration is not a member**: no `members` row, no mandate, nothing for `GET /api/sync/members` to return, so the terminal cannot recognise the person at all. Approval is the admin's attestation that the signed paper is in hand; reject deletes at once and an abandoned row purges itself after 14 days |
| [Encrypted Off-Site Database Backups](./2026-08-24-encrypted-offsite-backups.md) | In progress (M9 added 2026-09-04 — Microsoft's edge refuses the reference host's egress address outright, so ADR-0049's roadmap is reordered and a HiDrive WebDAV transport is built second rather than last, [#825](https://github.com/dgloeckner/clubbar/issues/825); M1–M4b merged into the integration branch; M5 complete, including the live-tenant run of the transport on 2026-08-26; M6 merged; M7 complete — the measured self-check rows ([#721](https://github.com/dgloeckner/clubbar/pull/721)), the panel banner's second cron line ([#724](https://github.com/dgloeckner/clubbar/pull/724)), the failure mail to the Admin ([#728](https://github.com/dgloeckner/clubbar/pull/728)) and the backups page; M8 not started) | [#686](https://github.com/dgloeckner/clubbar/issues/686), [ADR-0049](../adr/0049-encrypted-offsite-backups-on-shared-hosting.md): the installation had **no backups**, while the docs described a `mysqldump`/`gpg` procedure that cannot run on the reference host — unrunnable rather than merely absent, so a club reading it had every reason to think they were covered. The dump is therefore walked in PHP, sealed to public keys the server does not hold the other half of, written atomically, and pushed to the club's Microsoft 365 tenant behind a swappable `backup.dsn`. **No backup state lives in the database** (decision 8): the archive header is the record and the journal beside it is the history, because the earlier draft's three tables were self-referential in ways that bite — every archive contained its own half-written `running` row, and a compromise blocklist kept in the database was reverted by any restore predating the compromise. M6 is the milestone that decides whether the rest is a backup or a file: the real path is run, decrypted, restored into a second schema and compared **rows, schema and header claims**, then diffed again against `mariadb-dump` as an oracle — because a round trip compares the dumper against itself and a mistake made consistently in both directions survives it. The archive carries `config.php` as inert SQL comments, since restoring the rows alone yields a database every admin is locked out of: `security.totp_encryption_key` is not in the database and cannot be regenerated. Nothing reaches `main` before a restore has been proved |
| [Near-Limit Digest for the Treasurer](./2026-08-24-credit-limit-digest.md) | Implemented (M1–M6 done and each verified; backend PHPUnit 2935 green apart from one pre-existing, environment-only failure, vitest 490/490, `api-tests` mail-config 14/14, `admin-chromium` 25/25, `mail-digest` 2/2) | The treasurer is told, on a cadence they choose, which members are close to their Deckel ceiling — name, current Deckel and the limit that applies to them, in **one aggregate mail**. Who is near their ceiling was already computed and already correct ([#385](https://github.com/dgloeckner/clubbar/issues/385), [ADR-0047](../adr/0047-configurable-credit-limits.md)); what it was not was *reachable*, because the dashboard answers the question only once somebody opens it and asks. This is the same list pushed rather than pulled, riding [ADR-0038](../adr/0038-transactional-mail-outbox-on-shared-hosting.md)'s outbox as one more `kind` — a value and a builder, not a second queue and a second scheduler. Three decisions carry it: the **window goes in the `dedup_key`**, so a scheduler ticking every fifteen minutes produces one digest a week with no lookup and therefore no race between overlapping ticks; the **near-limit query moved into `CreditLimits`** so the panel and the mail cannot name different members (the band boundary is an integer division, and two spellings of it disagree exactly at the edge where every member on this list sits); and **nothing is queued when the list is empty**, so silence means "nobody is near their ceiling" rather than "the scheduler stopped". The queue row carries **no member data at all** — its subject is the singleton `credit_limit_config`, and the names and amounts are rebuilt at send time, so a digest queued on Monday and drained on Tuesday describes Tuesday. Recipients are `admin` and `kassenwart` only, mirroring the TREASURY route the dashboard panel sits on ([#633](https://github.com/dgloeckner/clubbar/issues/633)). [UC-A66](../use-cases/admin/UC-A66-credit-limit-digest.md) |
| [One Canonical Settlement Identifier](./2026-08-23-canonical-settlement-reference.md) | Implemented (M1–M7 done and each verified; backend 2802/2802, vitest 464/464, `api-tests` settlements 101/101, `mail-chain` 10/10) | A settlement carried four machine identifiers that disagreed with each other — a random `SEPA-<12 hex>` `MsgId`, a 16-hex `PmtInfId`, a doubly-truncated `EndToEndId` and the UUID in the admin URL — while the Verwendungszweck the member reads and the Vorabankündigung named nothing at all. Consolidated on one canonical rendering of `settlements.id` (32 lowercase hex, hyphens stripped), so the string a member reads off a bank statement pastes into the admin lookup and matches. `sepa_message_id` is dropped as a stored copy of a derivable fact; `EndToEndId` stays the single documented exception, because it must name a member as well as a run and still fit 35 characters. |
| [Per-Member Credit Limits](./2026-08-19-per-member-credit-limits.md) | Implemented (M1–M9 done and each verified; open as a stacked queue of PRs — [#659](https://github.com/dgloeckner/clubbar/pull/659) merged, then [#665](https://github.com/dgloeckner/clubbar/pull/665), [#667](https://github.com/dgloeckner/clubbar/pull/667), [#669](https://github.com/dgloeckner/clubbar/pull/669), [#671](https://github.com/dgloeckner/clubbar/pull/671), [#672](https://github.com/dgloeckner/clubbar/pull/672), [#673](https://github.com/dgloeckner/clubbar/pull/673), [#675](https://github.com/dgloeckner/clubbar/pull/675) and this one, all stacking into `feature/credit-limits`) | Epic [#555](https://github.com/dgloeckner/clubbar/issues/555) / [ADR-0047](../adr/0047-configurable-credit-limits.md) — the Deckel ceiling was already enforced ([#24](https://github.com/dgloeckner/clubbar/issues/24)); what it was not was **configurable**. The number lived as a constant in two languages — `app_config.dart` and a backend class — so a club wanting a different one needed a rebuilt kiosk binary and a backend deploy released *together*, or the dashboard's near-limit list and the Deckelauszug would start naming a figure nobody was holding. It becomes a club setting the Kassenwart edits, plus an optional per-member override, and the two resolve by one line — `override ?? clubDefault` — stated identically in PHP and Dart. **`NULL` and `0` are different answers**: NULL means *follow the club*, which is what makes raising the default lift everyone not deliberately excepted, while `0` means *no ceiling for this member* — `limitCents <= 0` has read as "not enforced" at the terminal since #24, so a negative value would pass that same test by accident and is refused by the API rather than stored. The warning band stays **club-wide**, one knob per member being what keeps the rule to a single duplicable line, and its boundary is integer division on all three sides (`intdiv`, `DIV`) so a kiosk and the dashboard cannot disagree by a cent. The club default needed its own transport: `/sync/members` is a delta on `updated_at` and a club setting touches no member row, so a synced terminal would never see the change — hence an authenticated `GET /api/sync/config`, and not `/health`, which carries only what a terminal needs *before* it can authenticate. The dashboard's near-limit list changed what it means as a result: it ranks by **share of ceiling** rather than raw amount, because with per-member ceilings the largest tab is no longer the closest to its limit. Enforcement is unchanged from [ADR-0020](../adr/0020-sepa-mandate-requirement-terminal-access.md) — the terminal prevents, the server records — and a failed `/sync/config` is non-fatal, leaving the last stored policy standing rather than reverting to a compiled-in number |
| [Mandatory Field Visibility](./2026-08-21-mandatory-field-visibility.md) | Implemented (M1–M4 done and each verified; vitest 394/394, api-tests 690/690, admin-chromium 335/335, admin-mobile 51/51, PHPUnit 2694/2695 with the one failure reproducing on a stashed tree) | [#629](https://github.com/dgloeckner/clubbar/issues/629) — the panel always knew which data was mandatory and never said so in a way anyone could act on. The member form's `*` becomes a three-tier marker — **required** (amber while empty, green once filled), **conditional** naming the capability a field unlocks ("erforderlich für Terminal-Zugang" says what an optional-but-load-bearing field is *for*, where "(optional)" said what it is not), and plain **optional** — with a summary that reports *every* missing field at once rather than the one native validation reaches first, `noValidate` being what buys that. The edit case gets the honest answer to "can a mandatory field be silently reset?": a required field cannot, because the save is now refused and the field named, but the three clearable ones could and did — a blank input has meant an explicit `null` since #131, so clearing a card UID revoked terminal access with nothing on screen saying so, and each now names the value it would delete and offers it back. The marker had already drifted (`CategorySelect` and friends rendered their own `*` on top of one the call site had appended, hence `Kategorie * *` beside a mandatory `Preis (€)` carrying none), so all three wrapper components render `FieldLabel` and no call site appends anything. And the roster learns to tell the story its four filter pills could not: a Datenqualität panel naming each gap, its count and what it costs — a member who cannot book, cannot be collected from, cannot be emailed, or is refused every age-restricted product — each button applying the filter **plus** `status=active` so the list holds exactly the members the number counted, and the SEPA-only column becoming a Daten column that reports all four from the same predicate the counts use. The birth-date gap needed the backend: `MemberListItem` gains `has_date_of_birth`, the **flag** and not the date, so ADR-0045's rule that a roster carries no birth dates survives while the one gap with a legal edge stops being invisible |
| [Admin Mail Reaches an Office](./2026-08-21-admin-mail-by-office.md) | Implemented (M1–M5 done and each verified; backend Unit 2072/2072 and Feature 672/672 green, `api-tests` 691/691, the whole mail chain 27/27 including the new `mail-roles` project) | [#633](https://github.com/dgloeckner/clubbar/issues/633) — `AdminNotifier::warnAdmins()` fanned every admin-addressed message out over *every active account*, so mail was the one surface [ADR-0044](../adr/0044-tiered-admin-roles.md)'s role model did not reach: an account holding only `getraenkewart` — refused the key screen, the terminals page, the admin list and the audit log — was mailed the club's encryption-key fingerprint, which tills had just been issued credentials, and who had just been promoted. A kind now states its offices in `MailKind::recipientRoles()`, beside `addressesMember()` and `addressesClub()`, as a `match` with no default so the next kind has to answer rather than inherit. The rule generating it is **mirror the grant on the surface the mail points at** — keys, terminals and admin accounts are `admin`-only routes, so their mail is `admin`-only, the Kassenwart included; `jugendschutz_violation` mirrors its `TREASURY` dashboard alert. The unstaffed-office case is a decision rather than whatever a filter happens to do: escalate to the club address, report `nobody_eligible`, and log a `WARNING` when there is no club address either — never fall back to every active admin, which is the leak itself arriving when the installation is least able to notice. The filter went into a **new** repository method: both mail builders resolve a greeting name for the `admin_user_id` already on an outbox row and must keep resolving whoever was actually written to, which a `WHERE` clause on the existing one would have blanked. Asserted where it matters against what a real drain delivered (Pattern 010), not against outbox rows |
| [Date Field](./2026-08-21-date-field.md) | Implemented (M1–M5 done and each verified; vitest 412/412, `admin-chromium` 333/333, `admin-mobile` 54/54) | Follow-up to [ADR-0045](../adr/0045-age-restricted-products.md): the birth date that plan made mandatory was being entered through `<input type="date">`, which is three different widgets across browsers and a bad one in all three for a date the user already knows — it opens on *today*, so 1979 is a hundred clicks on a chevron, and on a phone it is a spinner wheel starting at the current year. One `DateField` replaces every date input in the panel: typing first (`23111979` → `23.11.1979`, `1.1.1990` and a pasted ISO string both understood, 31 February refused rather than rolled into March), a calendar second where **month and year each have their own grid** so any date in living memory is three taps, a bottom sheet with 44px targets below 768px instead of a popover pinned to a field halfway up a scrolling modal, and `min`/`max` enforced in the field rather than by a 422. Locale decides what is shown (`Intl`, not a table of our own); ISO `YYYY-MM-DD` is what crosses every boundary, including a hidden input the E2E suite asserts on. Birth-date mode opens on the **year** view and shows the age the date implies — the number the terminal computes when it refuses a restricted drink, and the one that makes a mistyped year visible |
| [Jugendschutz: Age-Restricted Products](./2026-08-20-jugendschutz-age-restrictions.md) | Implemented (M1–M8 done and each verified; open as a stacked queue of PRs — [#606](https://github.com/dgloeckner/clubbar/pull/606), [#607](https://github.com/dgloeckner/clubbar/pull/607), [#608](https://github.com/dgloeckner/clubbar/pull/608), [#610](https://github.com/dgloeckner/clubbar/pull/610), [#616](https://github.com/dgloeckner/clubbar/pull/616), [#621](https://github.com/dgloeckner/clubbar/pull/621) and this one) | Epic [#582](https://github.com/dgloeckner/clubbar/issues/582) / [ADR-0045](../adr/0045-age-restricted-products.md) — the bar serves alcohol and nothing enforced **JuSchG § 9**. A member now carries a mandatory `date_of_birth`, a product an optional `min_age`, and the terminal refuses the combination **offline** — which is the only place it can be refused, because the kiosk is the only layer present when the drink is handed over. The raw date is synced rather than a derived age or flag: age changes on a day the server cannot predict a sync for, so anything precomputed is silently wrong from the member's next birthday until the next sync, and wrong in the direction that serves a minor. That puts one real personal-data field on every kiosk, which is stated rather than glossed — `erm-frontend.md`'s minimisation list and `terminal.yaml`'s "contains only non-sensitive data" claim both had to stop being true — and the compensating control is that erasure rides the same delta sync, so a birth date the server has erased survives on no kiosk beyond one sync interval. Enforcement **applies** [ADR-0020](../adr/0020-sepa-mandate-requirement-terminal-access.md)'s two-layer split rather than departing from it: the terminal prevents, and a sale a stale terminal let through is **stored and flagged**, never rejected — refusing the upload would not un-serve the minor, it would delete the club's knowledge that it happened and trade a youth-protection incident for a § 146 Abs. 1 AO bookkeeping one. The limit sits on the **product**, not the category, so rearranging the grid during a busy shift cannot silently un-restrict a drink; a NULL birth date means *anonymized* and refuses anything restricted, with no fail-open branch; the recorded violation keeps the limit as it stood at the sale and does not clear when the drink is later un-restricted; and the terminal never states a member's age — the refusal names what the **drink** requires. The Getränkewart sets that number and still takes 403 from the member roster. **M10** closed the last gap ([#622](https://github.com/dgloeckner/clubbar/issues/622)): M7 recorded the violation and told nobody, and the audit log holding it is `admin`-only — so the Kassenwart, the office the epic names as the recipient, could not learn one had happened. A dashboard alert and a member-free email now reach them, and each violation can be acknowledged individually. The record does not move when it is: the audit entry is immutable and the acknowledgement is a row beside it, so the incident stays on file forever while only the badge goes quiet — which is what keeps the badge worth reading. The club-website Art. 13 notice remains with [#591](https://github.com/dgloeckner/clubbar/issues/591) |
| [Tiered Admin Roles](./2026-08-17-tiered-admin-roles.md) | In Progress (M1–M8 implemented and each verified; merging as a stacked queue into `feature/tiered-admin-roles` — PRs #526–#532, #535, #536) | Epic [#513](https://github.com/dgloeckner/clubbar/issues/513) / [ADR-0044](../adr/0044-tiered-admin-roles.md) — the flat admin model of [ADR-0015](../adr/0015-authentication-and-authorization-strategy.md) (*"all admin users have full access"*) gains three hardcoded roles: `admin` (whoever holds the server), `kassenwart` (the elected treasurer — members, settlements, SEPA collection, Storno) and `getraenkewart` (the bar stock — drinks list, prices, product sales). The threat is **insider curiosity**, not session compromise, so what it contains is capability acquisition, escalation and persistence: a Kassenwart mints no accounts, no terminal tokens (and so no back door into the sync API that reads the full roster outside the panel), installs no encryption key of their own, cannot redirect the alert mail, and cannot read the audit log to see whether they are being watched. Roles compose additively, because in a volunteer club people cover for each other and single-role models degrade into shared credentials. **M1** shipped storage only and changed nobody's access: a relation rather than a `role` column, and a backfill that only ever *adds* `admin`. **M2** made it real — one declarative route→roles map consulted by `AdminSessionAuth`, where **a route with no entry is `admin`-only** and a completeness test over the real route collector fails until somebody classifies a new one. **M3** closed the hole the epic would otherwise have opened (creating an admin cost a password and a TOTP code while *promoting* one cost only a live session) and gave grants their own audit actions, written from the diff. **M4** allow-listed `group_by` on the reports endpoint, the one boundary that lands on a parameter. **M5** added the club-level notification address and the two lifecycle notices that reach it — the alarm that still fires when an installation has exactly one admin and they are the problem. **M6** is the panel: navigation a role cannot use is hidden rather than disabled, each office lands on a page it can open, and a page it may not opens a named refusal screen that also handles a role revoked mid-session. **M7** is the regression net — the grant table stated once as data and walked as each office over HTTP, plus each office's actual job run to completion. **M8** is the documentation |
| [RFID Scan Diagnostics](./2026-08-16-rfid-scan-diagnostics.md) | Implemented (881/881 terminal unit+widget tests; `flutter analyze` clean) | [#370](https://github.com/dgloeckner/clubbar/issues/370) — a review of the whole path from key event to member, and the answer to the issue's own note (*"might need debug logs"*). Everything from `emitScan` onward already makes a sound and shows something, so a tap with **no** reaction died in the capture stage, where nothing was recorded at all: a terminator that is the keypad's Enter and not Return, a reader typing keypad digits with NumLock off (no character, nothing buffered), a modifier the compositor thinks is still held, a burst the 500 ms gap timer cut in half, or a tap dropped while the previous lookup waits up to 3 s on the backend. `ScanLog` now records every one of those, with the silent losses at error level so they reach `error.log` — the only sink that outlives a kiosk session — and the last six in the status modal for staff to read out. Two fixes went with it: the keypad Enter terminates a scan, and a card UID **handed to another member** no longer wedges the entire sync (`members_cache.card_uid` is UNIQUE, the upsert conflicted on the primary key only, and the failure came out of the first step of the cycle before the cursor advanced — so the terminal silently stopped learning about any new member, product or chip until it was wiped) |
| [Terminal Credential Issuance Notice](./2026-08-16-terminal-credential-issuance-notice.md) | Implemented (M1–M6 done; backend unit + feature suites green, `api-tests` 634/634, `mail-issuance` 4/4, `mail-credentials` 3/3) | [ADR-0043](../adr/0043-terminal-credential-issuance-is-announced.md) — minting a terminal credential stops being the one credential event that creates a secret and tells nobody. Enrolment and rotation each queue a notice to **every active admin** through the ADR-0038 outbox, so the signal is out of band with respect to the credential that would have been compromised: whoever holds one admin's session, password and TOTP code clears the step-up on the mint path and still cannot reach the other mailboxes. Nothing about the mint path gets harder — ADR-0043 rejects gating it on ADR-0036's offline private key (a policy check rather than a cryptographic necessity, and it would make recovery harder than compromise) and defers device binding as in ADR-0041 §6. The event and the token's generation ride in the `dedup_key`, so a second rotation is a second message rather than one the unique index swallows, and the expiry is looked up *by* that generation across the pending and active columns. Extracting `AdminNotifier` out of `NotificationsService` fell out of it: the whole service drags `MembersRepository` → `IbanSealedBox` → a required `IBAN_FINGERPRINT_KEY`, which enrolling a terminal has no business needing |
| [Encryption Key Installation Hardening](./2026-08-16-encryption-key-installation-hardening.md) | Implemented (M1–M2 done; 786 backend tests green in the Security/Notifications/Mail scope) | Follow-up to [ADR-0036](../adr/0036-iban-encryption-sealed-box.md), from the question "what if someone installs a new public key and encrypts all IBANs with it?". Two gaps followed from a backend that can seal but never open: `register()` accepted **any** 32 well-formed bytes, so a mistyped public key activated exactly like a generated one and everything sealed afterwards was unreadable until the next export refused the archived key; and a key swap produced no signal that left the panel, because the expiry notifier covers only the 90/30/7 tiers and a key activated today has 365 days on it. Activation now carries the private half as proof — validated by public-key derivation, never trial decryption, wiped once compared — with `PrivateKeyMismatchException` keeping "no such key" (404) apart from "wrong sheet out of the safe" (422); and registering, activating and revoking each queue one outbox message per admin carrying the **full** fingerprint, the only value that distinguishes the club's key from someone else's since the identifier is free text. Neither stops a determined attacker, and the plan says so: proof of possession closes the typo case, mail is what makes the swap visible, and the change that would actually *prevent* it — a second admin approving activation — contradicts [ADR-0015](../adr/0015-authentication-and-authorization-strategy.md)'s flat model and is left as a follow-up |
| [Credential Expiry Notifications](./2026-08-16-credential-expiry-notifications.md) | Implemented (M1–M5 done; backend unit + feature suites green, `mail-credentials` 3/3) | [#438](https://github.com/dgloeckner/clubbar/issues/438) — the last deferred deviation in [ADR-0036](../adr/0036-iban-encryption-sealed-box.md) closes: the 90/30/7 expiry warnings for the IBAN encryption key and terminal tokens stop depending on somebody opening the admin panel and leave by email. The issue proposed hanging the trigger off an authenticated admin request; that cannot satisfy its own acceptance criterion — *"an admin who does not open the panel is still warned"* and *"the warning is queued by an admin opening the panel"* are the same sentence contradicting itself — and the premise behind it (no cron on shared hosting) expired when [ADR-0038](../adr/0038-transactional-mail-outbox-on-shared-hosting.md) made the scheduler **mandatory**. So the scan rides the existing tick before the drain, the tier goes in `dedup_key` and the unique index makes 90/30/7 three messages rather than one per tick for ninety days, terminals mid-rotation and switched-off tills are skipped, an expired credential is silent (expiry is already an error in the UI and a hard stop in the code), and an installation with no mail configured does nothing at all. The message names the remedy as the panel actually labels it — Einstellungen → Schlüssel, „Token rotieren" — and carries no key material, no token and no fingerprint, asserted against the bytes Mailpit received rather than against the template |
| [Banking Fields UX](./2026-08-16-iban-field-ux.md) | Implemented (M1–M5 done; rebased on 5cc04c9; admin-chromium 308/308, vitest 327/327, api member-blank-fields 14/14) | Follow-up to [#392](https://github.com/dgloeckner/clubbar/issues/392) — the member form catches up with [ADR-0036](../adr/0036-iban-encryption-sealed-box.md): where a banking value is already on file the form shows **it** instead of an empty input carrying a grey example, so "overwrite-or-keep" is visible rather than explained. The IBAN gains stored/entry/removing states behind explicit *Change* and *Remove* actions, the mandate reference gains assigned/auto/entry with a copy button and makes [ADR-0006](../adr/0006-sepa-mandate-reference-strategy.md)'s server-side assignment the default state, and the SEPA banner becomes a preview of what *this* submit would do instead of announcing "gültig" above the line saying the mandate is about to be revoked |
| [Admin Credential Hardening](./2026-08-15-admin-credential-hardening.md) | Implemented (M1–M5 done; 272 backend unit + 14 API + 5 UI tests green) | Follow-up to [#337](https://github.com/dgloeckner/clubbar/issues/337) — the step-up credential reaches self-service: changing your own password or email now re-proves password + TOTP, any credential change ends the account's other sessions via a new `credentials_changed_at` epoch (correcting [ADR-0026](../adr/0026-mandatory-totp-two-factor-authentication.md)'s enumeration rationale), the old address is notified, and `password_changed`/`email_changed` become their own audit actions. Also fixes a change-password form that could never have worked — frontend `POST … confirm_password` vs backend `PATCH … new_password_confirmation` — and a 401 interceptor that signed admins out on a mistyped step-up |
| [SEPA Mandate Template URL](./2026-08-15-sepa-mandate-template-url.md) | Implemented (M1–M7 done) | Follow-up to [#360](https://github.com/dgloeckner/clubbar/issues/360)/[PR #456](https://github.com/dgloeckner/clubbar/pull/456) — configurable `mandate_template_url` in SEPA config, blocking SEPA export while unset, a dashboard warning banner mirroring the IBAN-encryption-key one, and the Members page's "SEPA-Vorlage" button back as an external link instead of a PDF download |
| [SEPA Pre-Notification Emails](./2026-08-13-sepa-prenotification-emails.md) | In Progress (P0–P7 done — ADR-0038 + amendments, mail transport/layout/`mail_config`, the `mail_outbox` queue written inside the finalize transaction, the de/en announcement and cancellation content, the drain (`bin/cron.php`, `DrainService`, the `flock`, the URL fallback), the gate (**an installation whose scheduler has never been seen to run cannot finalize a direct debit**), and now the monitoring: an external push-monitor heartbeat per run, a stall alarm that keys on **overdue rather than age**, the scheduler's interval as a declared fact cross-checked against what the heartbeat observes (**`weekly` refused**), a retry ladder measured in scheduler ticks, and five measured `delivery` rows in the security self-check as the diagnosis surface — and now the admin surface: a **Notifications page** listing the queue with filters and a retry action, per-member announcement state in the settlement breakdown, a mail settings tab with the measured transport and a test-mail button, and a post-finalize confirmation that says *"queued, sending"* rather than *"sent"*. and now privacy and retention: erasure reaches the outbox inside the offboarding transaction, delivered rows are pruned at 90 days per kind by the drain, and a new `settlement_announcements` table keeps the address-free proof that a member was announced to for the retention period. and now the chain is asserted end to end: Mailpit in the dev stack and in CI, the drain triggered as `backend/bin/cron.php` in the container, and ten specs reading the **delivered message** — every field the Ordnung promises, the seven-day distance from the server's own today, the `en` variant and the `fr`→`de` fallback, two runs leaving one message, cancel-before and cancel-after, a 550 recorded and the retry after it delivering, and a real CLI run showing up as the verified scheduler. P10 withdrawn — a `bank_transfer` settlement is the record of money that already arrived, so nothing in this system ever asks a member to send money. **P11 done**: the Deckelauszug's machinery per [ADR-0039](../adr/0039-periodic-deckel-statement.md) — a *dated* `unsettledAsOf()` beside the live predicate, a calendar-anchored `StatementPeriod` whose key is the dedup key, `MailKind::DECKEL_STATEMENT` / `MailSubject::MEMBER`, the recipient scope (**everyone, including zero and credit balances; an inactive member who still owes; no address skipped silently**), and a scheduled enqueue `bin/cron.php` runs before the drain — idempotent by the unique index rather than by a lookup, so two ticks in a period leave one statement per member. **P12 done**: the Deckelauszug a member actually reads — netting, the 100-line cap and the total computed in **one pass over one set**, so lines and total cannot disagree; an orphaned Storno that names the settlement it came from; de/en content registered as a new `MailContentBuilder` rather than a branch in the drain, with negative assertions that no mandate reference, Gläubiger-ID, IBAN or due date can reach it; the `off | monthly | quarterly` switch in Settings → Mail; and the chain asserted over Mailpit in its own Playwright project, including two cron runs leaving exactly one message. **P13 done**: the scheduler dials stop describing one host — `CronInterval` gains the **15-minute** cadence `bin/cron.php` has recommended since P3 and that until now had to be declared as `hourly`, and the drain's wall-clock budget becomes a club-editable `mail_config.drain_budget_seconds` whose **default drops 50 → 25** so it is reachable inside an external scheduler's 30-second request timeout — a run killed mid-send has its rows handed back by the stale window and sends twice, while a run that stops early costs nothing but a tick. Task 3 of [#473](https://github.com/dgloeckner/clubbar/issues/473) (rotating the cron secret without file access) shipped early in [PR #472](https://github.com/dgloeckner/clubbar/pull/472)) | Epic [#361](https://github.com/dgloeckner/clubbar/issues/361) / [ADR-0038](../adr/0038-transactional-mail-outbox-on-shared-hosting.md) — announce every SEPA collection by email 7 days ahead (Nutzungsordnung § 7 Abs. 3) via a transactional outbox written inside the finalize transaction and drained by a mandatory scheduler, the only sending path. |
| [Reversal UI](./2026-08-14-reversal-ui.md) | Implemented (P0–P3 done; 17 admin E2E + 7 API lookup tests green, 295/295 frontend unit) | Epic [#433](https://github.com/dgloeckner/clubbar/issues/433) / [ADR-0032](../adr/0032-settlement-lifecycle.md) §8 — the #196 reversal backend reaches the admin panel: one undo slot per row that offers cancel *or* reverse, a reference lookup for recording a bank return, and the per-member breakdown behind an expandable row. Closes the UI half of [#81](https://github.com/dgloeckner/clubbar/issues/81); builds on [#377](https://github.com/dgloeckner/clubbar/issues/377) Phase 1 |
| [Instance Branding](./2026-08-11-instance-branding.md) | Implemented (M1–M6 done; PHPUnit 1448/1448, admin-chromium 284/284, Flutter 720/720 all green) | [ADR-0034](../adr/0034-instance-branding-configuration.md) — admin-configurable instance name (e.g. "FRGS Ruderbar"), set during install and editable in the admin panel, shown in the admin UI, Terminal UI, and as the TOTP issuer |
| [Excluded from Collection](./2026-08-09-excluded-from-collection.md) | Implemented (M0–M8 merged in [#261](https://github.com/dgloeckner/clubbar/pull/261); M9 — the third section, [#258](https://github.com/dgloeckner/clubbar/issues/258) — implemented, pending merge) | [#188](https://github.com/dgloeckner/clubbar/issues/188) — one Members-area surface for the two standing exclusions (members in credit, members on collection hold), desktop and mobile, plus the missing API coverage for both endpoints. Third bucket (no mandate) follows in [#258](https://github.com/dgloeckner/clubbar/issues/258) |
| [Settlement Member Selection](./2026-08-08-settlement-member-selection.md) | Implemented (M1–M4 done; Playwright pending CI) | [ADR-0030](../adr/0030-settlement-selection-is-a-member-picker.md) — settlement selection becomes a member picker on its own New Settlement screen, and leaves the Journal. The design half of [#128](https://github.com/dgloeckner/clubbar/issues/128), whose arithmetic half shipped in [#228](https://github.com/dgloeckner/clubbar/pull/228) |
| [Critical Remediation](./2026-08-06-critical-remediation.md) | Decisions substantially complete (21 rulings; 1 open on map #139) — **verification not started** | Sequenced fix plan for the 11 critical issues, each carrying its governing money-semantics ruling. ⚠️ Coordinate with Backend Test Coverage before its M2 — see issue #166 |
| [Backend Test Coverage](./2026-08-07-backend-test-coverage.md) | In Progress (M0 + M1 done — 15.11% → 26.64%; #103 merged in PR #154) | Issue #103 follow-up — raise PHPUnit line coverage from a measured 15.11% toward ADR-0022's 80%, starting with settlements/transactions (money), auth (security), members (GDPR) |
| [SEPA NOTPROVIDED BIC Encoding](./2026-08-05-sepa-notprovided-bic-encoding.md) | In Progress (Tasks 1-4 done; bank file check pending) | Issue #12 — emit `Othr/Id = NOTPROVIDED` instead of `BICFI = NOTPROVIDED` for IBAN-only agents in the pain.008.001.08 export |
| [SEPA Execution Date: Bank Business Day Rule](./2026-08-05-sepa-execution-date-business-day.md) | Implemented (E2E green; A7 follow-up [#113](https://github.com/dgloeckner/clubbar/issues/113) implemented, pending merge) | Issue #11: reject non-business-day `execution_date` (422, weekends + six TARGET2 closing days) on both settlement endpoints, add `GET /settlements/execution-date-info` as the single source of truth, guard the SEPA export. #113 then re-anchored the 7-day lead time on the server's today and removed `settlement_date` from the request |
| [Terminal: Checkout Double-Tap Guard](./2026-08-05-checkout-double-tap-guard.md) | Implemented | Issue #14: re-entrancy guard in `CartProvider.checkout` plus a disabled/loading checkout button, so a double-tap can no longer create duplicate transactions |
| [Terminal: RFID Reader Health Detection](./2026-08-10-rfid-reader-health-detection.md) | Implemented (582/582 unit + widget tests green) | [#35](https://github.com/dgloeckner/clubbar/issues/35): poll `/proc/bus/input/devices` for the configured reader, surface it as a third header pill, and replace the idle screen's invitation with "Scanner nicht verbunden" while the reader is gone |

---

## Previous Current Plans

| Plan | Status | Summary |
|------|--------|---------|
| SEPA Vision OCR Pipeline (plan document not retained) | Completed | Replace multi-pass LLM extraction with Google Vision OCR + Haiku text pipeline; adds street/zip/city fields, checksumValid, needsReview |
| [Reset 2FA Button](./2026-03-23-reset-2fa-button.md) | Completed | Add "Reset 2FA" icon button to AdminUsersTab with confirm dialog, API helper, E2E test |
| [TOTP + Session Bug Fixes](./2026-03-23-totp-session-bugfixes.md) | Not Started | Fix SESSION_MAX_AGE not applied to PHP session engine; fix frontend enrollment flow showing dashboard instead of QR code |
| [Install TOTP Key Generation](./2026-03-22-install-totp-key-generation.md) | Completed | Auto-generate TOTP_ENCRYPTION_KEY in package installer (config.sample, install.php, index.php) |
| [Backend Security Review](./2026-03-17-backend-security-review.md) | In Progress | C1/C2/C3/H4 done; H3/H6/M3 this sprint; H1/H2/H5/M1/M2/M4/L1–L4 next sprint |
| [GDPR Anonymization Fix](./2026-03-08-gdpr-anonymization-fix.md) | Not Started | Fix anonymization: NULL instead of DELETED, audit log scrubbing, pre-deletion checks, E2E tests |
| [CI Pipeline Refactoring](./2026-03-07-ci-pipeline-refactoring.md) | In Progress | Restructure CI: job dependencies, caching, dedup, lint gate, fix silent failures |

---

## Completed Plans

| Plan | Completed | Summary |
|------|-----------|---------|
| [Anmeldelink: mail the club's registration link](./2026-09-04-anmeldelink.md) | Implemented (M1–M7 done and each verified; backend Unit 3038/3038, vitest 598/598, `api-tests` self-registration 56/56, `admin-chromium` registrations-inbox 12/12, `register` 38/38, `mail-anmeldelink` chain 3/3) | [#821](https://github.com/dgloeckner/clubbar/issues/821): the QR poster reaches only people standing in the clubhouse, so a club that meets somebody at a regatta has a Kassenwart pasting the URL into private WhatsApp. The club sends it instead — **the poster's own URL, carrying no credential of its own**, because the secret is already printed on a public wall. No token, no expiry, no revocation, no invitee table: the outbox row is the whole record. Uncovered a third mail audience — "not addressed to a member" had silently meant "fanned out to admins", and `warnAdmins()` would have accepted this kind and queued nothing. M7 ([#823](https://github.com/dgloeckner/clubbar/issues/823)) made the link fill in the address it was sent to: it rides in the **fragment** beside the secret, because an address in a query string is personal data written into every access log in front of the installation, and it comes from the outbox row's own `recipient` — nothing new stored, the poster's URL byte-for-byte unchanged |
| [Delete an Admin Account That Never Signed In](./2026-09-03-delete-admin-account.md) | 2026-09-03 | [UC-A61](../use-cases/admin/UC-A61-manage-admins.md) — the Administrators tab had no way to remove an account, so an invitation sent to the wrong address stayed on the roster forever, and `DELETE /admin-users/{id}` had always *deactivated* rather than deleted. `DELETE` now removes the row and deactivation moved to `POST .../deactivate`, beside the `/reactivate` it pairs with (both, plus `GET /{id}`, were undocumented in `api/admin.yaml`, which is why the generated client had no delete to call). Deletion is offered only for an account that has never signed in **and** never authored an audit row: `settlements` and `mandate_documents` reference their creating admin with `ON DELETE RESTRICT` while `audit_log.admin_user_id` carries no constraint at all, so a wider delete would either be refused by the database or silently blank the actor on every row that admin ever wrote (ADR-0013). The audit half is not redundant — accepting an invitation audits under the invitee's own name before any `last_login_at` exists. The trash button is withheld from rows that do not qualify rather than offered and then refused, the same treatment the own-row switch gets (#382), and deleting an account cascades away its outstanding invitation link. No new ADR: ADR-0044's grant table already named delete an `admin` capability |
| [Member Lifecycle Mail](./2026-08-29-member-lifecycle-mail.md) | 2026-08-29 | [ADR-0051](../adr/0051-member-lifecycle-mail.md) / [UC-A67](../use-cases/admin/UC-A67-member-lifecycle-mail.md) — the first mail a member gets for a reason other than money: a welcome when their **card** is assigned (not when the record is created), a notice when a later card replaces it, and both ends of an address change. Closes the gap #362 left: an address made mandatory because the Vorabankündigung is a § 7 Abs. 3 promise, then only format-checked — the copy to the new address is the only thing that ever tests one, and its bounce lands months before a collection depends on it. Welcome-vs-replacement is read from the transition so `MailRetention` pruning cannot turn a replacement back into a greeting; neither address notice names the other address, because bodies render at send time. Also closed drift found on the way: the OpenAPI enum, the Notifications filter and both locale maps were each missing ten existing kinds |
| [SQL Performance Improvements](./2026-08-18-sql-performance-improvements.md) | 2026-08-18 | Query-plan audit against a 600k-row seeded `transactions` table: `idx_transactions_occurred_at` and `idx_audit_entity_action_created` turn four full-table-scan repository methods (dashboard, reports, the admin transactions journal, the audit-log dedup check) into index range scans — the journal's default view alone went from ~467ms to ~0.2ms. A tempting `(transaction_type, occurred_at)` composite was built, measured, and rejected: it regressed wide-date-range reports even after fresh `ANALYZE TABLE` stats. Also fixed `AuditLogRepository::listWithFilters()` wrapping `created_at` in `DATE()` (which made the existing index unusable). Investigated `sessions` table growth per the request; found it isn't a pruning gap but dead code — nothing writes to that table at all, since admin auth runs on native PHP file sessions (`RuntimeHardening`) — so no fix shipped there, only the finding. A follow-up audit of every rate-limiting mechanism's SQL (login, MFA, step-up re-auth, terminal token auth) found the queries themselves already optimal (sargable, index-covered `COUNT(*)` scans) but two of the four backing tables — `login_attempts`, `terminal_auth_attempts` — had no pruning at all; added `LoginAttemptsRepository::pruneOlderThan()` (mirroring `TerminalIpSightingsRepository`'s existing pattern) wired into `cron.php` |
| [Remove Mandate Scan Extraction](../adr/0040-remove-mandate-scan-extraction.md) | 2026-08-15 | [ADR-0040](../adr/0040-remove-mandate-scan-extraction.md) (amends [ADR-0037](../adr/0037-mandate-documents-not-retained.md)) — removed the LLM/Vision-based "New from scan" mandate extraction feature entirely: backend extraction services and LLM/Vision clients, the admin panel's scan UI, the API endpoint and schemas, config/env vars, and tests. Datenschutz risk of sending IBAN/signature scans to third-party APIs plus a persistently high error rate outweighed the convenience; admins now enter member data, including IBAN, by hand |
| [IBAN Encryption (Sealed Boxes)](./2026-08-13-iban-encryption-sealed-box.md) | 2026-08-14 | Epic [#388](https://github.com/dgloeckner/clubbar/issues/388) / [ADR-0036](../adr/0036-iban-encryption-sealed-box.md) + [ADR-0037](../adr/0037-mandate-documents-not-retained.md) — P0–P8 done: IBANs encrypted at rest with libsodium sealed boxes (server holds public key only; private key offline with the club, supplied per SEPA export/rotation behind TOTP step-up), 365-day key + terminal-token lifecycle with 90/30/7 warnings, last4-only API surfaces, mandate scans extraction-only with no retention, docs + backup/recovery procedures + follow-up issues ([#437](https://github.com/dgloeckner/clubbar/issues/437), [#438](https://github.com/dgloeckner/clubbar/issues/438)) closing out the epic |
| [IONOS Integration Deployment](../docs/superpowers/plans/2026-03-29-ionos-deployment.md) | 2026-03-29 | Self-destructing deploy.php migration runner + deploy-integration CI job |
| [SEPA Mandate LLM Extraction](../docs/superpowers/plans/2026-03-27-sepa-mandate-extraction.md) | 2026-03-27 | LLM extraction of member fields from mandate scans; Anthropic/OpenAI via cURL; confidence badges; create-member-from-scan flow |
| [SEPA Mandate Upload](../docs/superpowers/plans/2026-03-26-sepa-mandate-upload.md) | 2026-03-27 | Upload scanned mandate documents per member (JPEG/PNG/HEIC/PDF), client-side compression, server-side PDF conversion, view/replace/delete, GDPR anonymization integration |
| [E2E Coverage Gaps](./2026-03-23-e2e-coverage-gaps.md) | 2026-03-23 | Fix broken export URLs in 3 test files, fix meaningless assertions, add UI-driven export/correction/confirm tests |
| [Mandatory TOTP 2FA](./2026-03-22-totp-2fa.md) | 2026-03-22 | Mandatory TOTP 2FA for all admins: backend, OAS, frontend, E2E tests (364 passing) |
| [Security Critical Fixes](./2026-03-17-security-critical-fixes.md) | 2026-03-17 | C1 timing attack, C2 wildcard CORS, C3 APP_DEBUG=true — all fixed |
| [Terminal Auth Rate Limiting](../docs/superpowers/plans/2026-03-17-terminal-rate-limiting.md) | 2026-03-17 | IP-based rate limiting on terminal sync endpoints — 10 failures/15 min → 429 |
| [install.php Security Hardening](./2026-03-17-install-php-security-hardening.md) | 2026-03-17 | Fix Env::get() precedence bug, remove key from query param, block install.php via .htaccess |
| [Dashboard Page](./2026-03-08-dashboard-page.md) | 2026-03-08 | Dashboard page (UC-A80): metrics, transactions, terminals, alerts, auto-refresh, mobile responsive |
| [Reports System](./2026-03-07-reports-system.md) | 2026-03-08 | Reports system (UC-A50, UC-A51, UC-A52): backend API, frontend ReportsPage, responsive, E2E tests |
| [ADR Resolution](./2026-03-07-adr-resolution.md) | 2026-03-07 | Resolved all ADR gaps: CSRF, rate limiting, ADR rewrites, use case cleanup |
| [ADR Implementation Status](./adr-implementation-status.md) | 2026-03-07 | All 23 ADRs resolved (22 implemented + 1 superseded) |
| [Use Case Implementation Audit](./use-case-implementation-audit.md) | 2026-03-07 | Audit of all use cases against implementation |

---

## How to Use This Index

1. **Starting work**: Check "Current Plan" for active work
2. **New feature**: Create a plan in `plans/`, add to this index
3. **Completing a plan**: Move from Current to Completed with date
