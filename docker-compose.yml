# Ruderbar - Development Environment
# Usage: docker-compose up -d

services:
  # =============================================================================
  # Database - MariaDB
  # =============================================================================
  database:
    image: mariadb:10.11
    container_name: ruderbar-db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: ruderbar
      MYSQL_USER: ruderbar
      MYSQL_PASSWORD: ruderbar
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql
      - ./docker/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 5

  # =============================================================================
  # Backend - PHP 8.3 + Apache (serversideup/php image, code mounted)
  # Setup: cd backend && composer install && cd ..
  # Start:  UID=$(id -u) GID=$(id -g) docker compose up -d
  # Docs:   backend/DOCKER_SETUP.md, backend/FILE_PERMISSIONS.md
  # =============================================================================
  backend:
    image: webdevops/php-apache:8.3
    container_name: ruderbar-backend
    restart: unless-stopped
    environment:
      PUID: ${UID:-1000}
      PGID: ${GID:-1000}
      # Apache Configuration
      WEB_DOCUMENT_ROOT: /app/public
      SSL_MODE: "off"

      # PHP Configuration
      PHP_MEMORY_LIMIT: "256M"
      PHP_MAX_EXECUTION_TIME: "99"
      PHP_UPLOAD_MAX_FILE_SIZE: "100M"
      PHP_POST_MAX_SIZE: "100M"
      PHP_OPCACHE_ENABLE: "0"  # Disabled for development

      # Laravel Configuration
      APP_ENV: local
      APP_DEBUG: true
      APP_KEY: base64:6i7N5vhbwJd1aAuAMK2e5UhDwaaBIJ04lkodIjrQmZ0=
      APP_URL: http://localhost:8080

      # Database Configuration
      DB_CONNECTION: mysql
      DB_HOST: database
      DB_PORT: 3306
      DB_DATABASE: ruderbar
      DB_USERNAME: ruderbar
      DB_PASSWORD: ruderbar

      # Laravel Automations (disabled for Phase 1)
      AUTORUN_ENABLED: "false"
    ports:
      - "8080:80"
    volumes:
      - ./backend:/app
      - ./docker/apache-laravel.conf:/opt/docker/etc/httpd/vhost.common.d/01-laravel.conf:ro
    depends_on:
      database:
        condition: service_healthy

  # =============================================================================
  # Admin Frontend - React + Apache (production-like, mass hoster compatible)
  # =============================================================================
  admin-frontend:
    build:
      context: .
      dockerfile: docker/admin-frontend.Dockerfile
    container_name: ruderbar-admin
    restart: unless-stopped
    profiles:
      - frontend
    ports:
      - "5173:80"
    depends_on:
      - backend

  # =============================================================================
  # Terminal Frontend - React + Vite (dev server for web preview)
  # =============================================================================
  terminal-frontend:
    image: node:20-alpine
    container_name: ruderbar-terminal
    working_dir: /app
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
    profiles:
      - frontend
    ports:
      - "5174:5174"
    volumes:
      - ./terminal:/app
      - terminal_node_modules:/app/node_modules
    depends_on:
      - backend

  # =============================================================================
  # E2E Tests - Playwright
  # =============================================================================
  e2etests:
    image: mcr.microsoft.com/playwright:v1.49.0-noble
    container_name: ruderbar-e2etests
    working_dir: /app
    profiles:
      - test
    environment:
      API_URL: http://backend
      ADMIN_URL: http://admin-frontend
      TERMINAL_URL: http://terminal-frontend:5174
    volumes:
      - ./e2etests:/app
      - e2etests_node_modules:/app/node_modules
    depends_on:
      - backend
    command: sh -c "npm install && npx playwright test --project=api-tests"

volumes:
  db_data:
  terminal_node_modules:
  e2etests_node_modules:
